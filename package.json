{
  "name": "Hotel-reservation-app",
  "lockfileVersion": 3,
  "requires": true,
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "packages": {},
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace frontend\" \"npm run devStart --workspace backend\"",
    "install-all": "npm install"
  }
}
